<UserControl x:Class="ModernThinkPadLEDsController.Presentation.Controls.LabelWithHelp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30"
             d:DesignWidth="200">
    <StackPanel Orientation="Horizontal"
                HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                VerticalAlignment="{Binding VerticalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center"/>
        <Border Margin="7,0,0,0"
                Width="16"
                Height="16"
                CornerRadius="8"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                VerticalAlignment="Center"
                Cursor="Help"
                ToolTipService.ShowDuration="20000"
                ToolTipService.InitialShowDelay="100"
                Tag="{Binding HelpText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Visibility="{Binding HelpVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Border.ToolTip>
                <ToolTip>
                    <TextBlock MaxWidth="280"
                               TextWrapping="Wrap"
                               Text="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ToolTip}}"/>
                </ToolTip>
            </Border.ToolTip>
            <TextBlock Text="?"
                       FontSize="10"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </Border>
    </StackPanel>
</UserControl>
