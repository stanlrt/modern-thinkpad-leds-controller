<UserControl x:Class="ModernThinkPadLEDsController.Presentation.Controls.LabeledSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="400">
    <StackPanel>
        <!-- Title -->
        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <!-- Slider + Value Display -->
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center">
            <Slider Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Width="200"
                    Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    TickFrequency="{Binding TickFrequency, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    IsSnapToTickEnabled="True"/>
            <!-- <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0}}"
                       VerticalAlignment="Center"
                       Width="Auto"
                       Margin="8,0,0,0"/> -->
            <TextBox Width="Auto"
                     Margin="8,0,0,0"
                     Padding="4,2"
                     PreviewTextInput="NumberValidationTextBox"
                     LostFocus="TextBox_LostFocus"
                     x:Name="ValueTextBox"
                     Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0}, UpdateSourceTrigger=PropertyChanged}">
                <Validation.ErrorTemplate>
                    <ControlTemplate/>
                </Validation.ErrorTemplate>
            </TextBox>
            <TextBlock Text="{Binding Unit, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       VerticalAlignment="Center"
                       Margin="4,0,0,0"/>
        </StackPanel>

        <!-- Validation Error -->
        <TextBlock Text="{Binding ValidationError, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="11"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,4,0,0"
                   Visibility="{Binding ValidationErrorVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        <!-- Description -->
        <TextBlock Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="11"
                   Opacity="0.6"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"
                   Visibility="{Binding DescriptionVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </StackPanel>
</UserControl>
