<Application x:Class="ModernThinkPadLEDsController.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- wpfui requires these two dictionaries to apply its Fluent theme.
                     ThemesDictionary provides colours; ControlsDictionary provides
                     control templates (what a Button, CheckBox, etc. look like). -->
                <ui:ThemesDictionary Theme="Dark"/>
                <ui:ControlsDictionary/>

                <!-- Theme-specific resource overrides (LightTheme.xaml or DarkTheme.xaml) -->
                <!-- are added here dynamically by App.xaml.cs based on system theme -->
            </ResourceDictionary.MergedDictionaries>

            <!-- ── Hyperlink: Use accent color instead of default blue ── -->
            <Style TargetType="Hyperlink">
                <Setter Property="Foreground"
                        Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                <Setter Property="TextDecorations"
                        Value="None"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver"
                             Value="True">
                        <Setter Property="TextDecorations"
                                Value="Underline"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ── TabItem: WinUI3-style with accent underline on selected tab ── -->
            <Style TargetType="TabItem">
                <Setter Property="Foreground"
                        Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <!-- Outer border provides the hover background -->
                            <Border x:Name="Root"
                                    Background="Transparent"
                                    CornerRadius="4"
                                    Margin="0,0,2,0"
                                    Padding="12,6,12,0">
                                <!-- Inner Grid stacks label + underline bar -->
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="3"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0"
                                                      ContentSource="Header"
                                                      Margin="0,0,0,6"
                                                      RecognizesAccessKey="True"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    <!-- Accent underline — hidden by default -->
                                    <Border x:Name="Underline"
                                            Grid.Row="1"
                                            Height="3"
                                            CornerRadius="1.5"
                                            Margin="4,0,4,0"
                                            Background="{DynamicResource AccentFillColorDefaultBrush}"
                                            Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected"
                                         Value="True">
                                    <Setter TargetName="Underline"
                                            Property="Visibility"
                                            Value="Visible"/>
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver"
                                                   Value="True"/>
                                        <Condition Property="IsSelected"
                                                   Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="Root"
                                            Property="Background"
                                            Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
